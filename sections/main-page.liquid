<div id="page" class="desktop-12 tablet-6 mobile-3 pagecontent" data-section-id="{{ section.id }}">
  <h1 class="text-center">{{ page.title }}</h1>
  {{ page.content }}
</div>
{%- if page.handle contains 'about' -%}
  <style type="text/css">
    .pagecontent p{
     font-size:17px;
    }
  </style>
{%- endif -%}
{% schema %}
  {
    "name":"Main Page"
  }
{% endschema %}